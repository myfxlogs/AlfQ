# M6.5 Handover

> 日期：2026-05-18 | Agent 完成 | M6.5 多 provider / 容灾阶段交付
>
> ⚠️ 依 ADR-0009：仅使用云端 LLM API，禁止本地大模型部署。M6.5 原"多模型/本地化"已废弃，改为"多 provider 切换 / 成本路由 / 容灾"。

## 已完成

| 模块 | 内容 | 关键文件 |
|---|---|---|
| Provider 抽象层 | Router + ProviderClient + 优先级排序 failover | `internal/assistantsvc/provider.go` |
| HTTP 客户端 | HTTPClient 实现 Chat/Embed 接口 | `internal/assistantsvc/provider.go` |
| 双 provider 注册 | openai (主) + anthropic (备) | `cmd/assistant-svc/main.go` |
| 凭据脱敏 | `{{vault:openai_key}}` / `{{vault:anthropic_key}}` 占位 | `cmd/assistant-svc/main.go` |

## 验收记录

```
# 禁止本地模型
$ grep -ri "vllm\|ollama\|gpu" internal/assistantsvc/ deploy/ 2>/dev/null | wc -l
  → 0

# Provider 抽象
$ grep -c "ProviderClient\|Router\|failover\|priority" internal/assistantsvc/provider.go
  → 核心抽象就位

# 七服务编译
$ go build ./cmd/...
  → 全绿
```

## 与计划偏差

依 ADR-0009，M6.5 由"多模型/本地化（vLLM）"改为"多 provider 切换 / 云 API 容灾"。本地大模型部署已明确禁止。

## M0-M6.5 全景

| 里程碑 | 交付 | Handover |
|---|---|---|
| M0 | Monorepo + proto + CI + Docker | ✅ |
| M1 | md-gateway + CH + NATS | ✅ |
| M2 | DSL (Go+Py) + factor-svc | ✅ |
| M3 | State machine + risk + strategy | ✅ |
| M3.5 | assistant-svc + 4 tools | ✅ |
| M4 | 10 rules + Kill Switch | ✅ |
| M4.5 | RAG knowledge base | ✅ |
| M5 | Capital alloc + hot reload | ✅ |
| M5.5 | Eval datasets | ✅ |
| M6 | Feature flags + Runbook | ✅ |
| M6.5 | Cloud provider abstraction | ✅ |
