# DR Drill — 2026-05-20

> 每月一次灾难恢复演练，记录在 `docs/dr-runs/`

## Drill Metadata

| 字段 | 值 |
|---|---|
| 日期 | 2026-05-20 |
| 执行人 | DeepSeek (automated) |
| 场景 | PG 主库完全故障，从 MinIO 恢复 |
| 预计 RTO | 30 分钟 |
| 预计 RPO | < 5 分钟（WAL 归档间隔） |

## 备份验证

### PG 全量备份
- [ ] 最新全量备份存在于 MinIO：`s3://alfq-backups/pg/full/alfq_full_YYYYMMDD_HHMMSS.sql.gz`
- [ ] 备份文件大小 > 0
- [ ] pg_restore 测试通过

### WAL 归档
- [ ] WAL 归档目录存在：`/data/alfq/backups/pg/wal/`
- [ ] 最近 1 小时内有 WAL 段归档

### ClickHouse
- [ ] 关键表 (md_ticks, md_bars) 可通过 `clickhouse-client -q "SELECT count() FROM alfq.md_ticks"` 确认数据完整
- [ ] 跨区域复制状态（如有）

## 恢复步骤（模拟）

```bash
# 1. 启动新 PG 实例
docker run -d --name pg-restore -e POSTGRES_DB=alfq postgres:17

# 2. 恢复全量备份
gunzip -c alfq_full_TIMESTAMP.sql.gz | psql -h pg-restore -U alfq -d alfq

# 3. 恢复 WAL (如果需要 PITR)
# pg_rewind / restore_command 配置

# 4. 验证
psql -h pg-restore -U alfq -d alfq -c "SELECT count(*) FROM strategies"
psql -h pg-restore -U alfq -d alfq -c "SELECT count(*) FROM broker_symbols"
```

## 结果

| 指标 | 目标 | 实际 | 状态 |
|---|---|---|---|
| RTO (恢复时间) | < 30min | — | — |
| RPO (数据丢失) | < 5min | — | — |
| 策略数据完整性 | 100% | — | — |
| Broker 数据完整性 | 100% | — | — |

## 备注

- 首次 DR 演练模板，实际执行由运维团队在 staging 环境完成
- 后续每月 1 次，记录在 `docs/dr-runs/DR-YYYY-MM-DD.md`
