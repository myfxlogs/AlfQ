# 未完成功能实施计划

> 日期：2026-05-22 | 基线：REMEDIATION-PLAN 37/37 ☑ | MASTER-ROADMAP  A-E 阶段主体完成

---

## 状态标记

| 标记 | 含义 |
|---|---|
| ☐ | 未开始 |
| 🔧 | 进行中 |
| ☑ | 验收通过 |
| 🅒 | code-only，待真实验收 |

---

## 第一梯队：质量闭环（4 项，预计 5 天）

> 目标：消除 🅒，建立可验证的质量基线。

### Q1 · testcontainers 集成测试

| 字段 | 内容 |
|---|---|
| ID | **DP-1.2** |
| 来源 | MASTER-ROADMAP |
| 当前 | ☑ |
| 预计 | 3 天 |
| 依赖 | 无 |

**范围**
- 使用 `testcontainers-go` 启动 PG/Redis/NATS/ClickHouse 容器
- 编写关键链路集成测试：`CreateStrategy → Deploy → Paper → Live`
- 覆盖 adminapi handler 的 PG 读写路径
- CI 中 `test.yml` 添加 `go test -tags=integration ./internal/adminapi/...`

**验收**
```bash
go test -tags=integration -count=1 ./internal/adminapi/... && echo "PASS"
# 期待 0 FAIL，adminapi 覆盖率从 3.4% → >40%
```

---

### Q2 · symbol fixtures 真实化

| 字段 | 内容 |
|---|---|
| ID | **SM-1.1** |
| 来源 | MASTER-ROADMAP |
| 当前 | ☑ |
| 预计 | 1 天 |
| 依赖 | 无 |

**范围**
- 从现有 broker_symbols 表导出 fixtures JSON
- 替换 `DefaultSymbolResolver()` 中的硬编码 map
- 无 PG 时回退到 fixtures

**验收**
```bash
grep -c "EURUSD\|GBPUSD\|USDJPY" backend/go/internal/quantengine/signal_oms_bridge.go
# 期待 >0（使用 fixtures 而非硬编码）
```

---

### Q3 · ONNX runtime 文档

| 字段 | 内容 |
|---|---|
| ID | **EP-2.1** |
| 来源 | MASTER-ROADMAP |
| 当前 | ☑ |
| 预计 | 0.5 天 |
| 依赖 | 无 |

**范围**
- 编写 `docs/adr/0013-onnx-runtime-strategy.md`（ADR 0013 正式文档）
- 内容：ONNX 模型选择理由、训练/推理流程、因子→特征映射、版本管理

**验收**
```bash
test -f docs/adr/0013-onnx-runtime-strategy.md && echo "EXISTS"
```

---

### Q4 · SLO dashboards + runbook + 灾备

| 字段 | 内容 |
|---|---|
| ID | **OP-1/2/3** |
| 来源 | MASTER-ROADMAP |
| 当前 | ☑ |
| 预计 | 0.5 天 |
| 依赖 | 无 |

**范围**
- `deploy/prometheus/rules/alerts.yml`：SLO 告警规则（延迟 > 500ms / 错误率 > 1%）
- `deploy/grafana/dashboards/slo.json`：SLO 仪表盘
- `docs/runbook/incident-playbooks.md`：已有，补充 MT 断连 / 因子异常 / 回测失败三个场景
- `scripts/backup-pg.sh`：已有，补充恢复验证步骤

**验收**
```bash
test -f deploy/prometheus/rules/alerts.yml && echo "alerts OK"
test -f deploy/grafana/dashboards/slo.json && echo "dashboard OK"
grep -c "MT 断连\|因子异常\|回测失败" docs/runbook/incident-playbooks.md
```

---

## 第二梯队：架构增强（4 项，预计 15 天）

> 目标：完成 RS04-RS08，消除技术债务。

### A1 · broker_sim 真实化

| 字段 | 内容 |
|---|---|
| ID | **RS04** |
| 来源 | REMEDIATION-PLAN |
| 预计 | 3-4 天 |
| 依赖 | 无 |

**范围**
1. `BrokerSim` 从 `broker_symbols` 读真实 spread/swap/commission
2. 修正 swap 公式按 `swap_mode` 分支
3. `SpreadModel`：从 CH 历史 tick 计算分时段 spread 分布
4. `FillModel`：部分成交 + SL/TP 判定
5. 三档参数：`OptimisticFee` / `RealisticFee` / `ConservativeFee`

**验收**
```bash
# SMA cross 策略回测 EURUSD 2024，Sharpe 与公开实盘偏差 < 0.3
cd research && uv run pytest tests/test_backtest.py -k "sharpe" -v
```

---

### A2 · StrategyRuntime 重写

| 字段 | 内容 |
|---|---|
| ID | **RS05** |
| 来源 | REMEDIATION-PLAN |
| 预计 | 5 天 |
| 依赖 | RS01 + RS02 |

**范围**
1. 每策略一 goroutine + recover 隔离
2. 事件驱动：从 NATS subscribe `md.bar.>` 投递到 per-runtime channel
3. `Snapshot()` 每 N 秒写 PG `strategy_runtime_snapshots`
4. hot-reload：监听 `strategy_revisions` NOTIFY，灰度切流
5. metrics 全部带 `strategy_id` label

**验收**
```bash
# 启动 5 个策略，kill 1 个 panic，其余 4 个正常运行
# 重启 quant-engine，所有策略从 snapshot 恢复
docker logs deploy-quant-engine-1 | grep "runtime restored"
```

---

### A3 · RLS middleware 统一注入

| 字段 | 内容 |
|---|---|
| ID | **RS07** |
| 来源 | REMEDIATION-PLAN |
| 预计 | 1 天 |
| 依赖 | RC02 |

**范围**
1. Connect interceptor 统一从 ctx 取 tenant_id → `SET LOCAL app.tenant_id`
2. 所有 handler 移除手工 `setRLS` 调用
3. lint 规则：禁止直接 `s.pool.Query`

**验收**
```bash
grep -r "setRLS" backend/go/internal/adminapi/ && echo "STILL MANUAL" || echo "CLEAN"
```

---

### A4 · mthub 真正收敛

| 字段 | 内容 |
|---|---|
| ID | **RS08** |
| 来源 | REMEDIATION-PLAN |
| 预计 | 3-5 天 |
| 依赖 | 无 |

**范围**
1. SymbolParamsMany / PriceHistory 从 stub 改为真实 MT 调用
2. accountconn 移除直连 mtapi，改为调 mthub RPC
3. mthub 统一 metrics / 限流 / 重试

**验收**
```bash
grep -r "mtapi\." backend/go/internal/ | grep -v "/mthub/" && echo "LEAK" || echo "CLEAN"
```

---

## 执行顺序

```
Q2 (fixtures) ──┐
Q3 (ONNX doc)  ─┤
Q4 (SLO/runbook)┘── 可并行，预计 1 天
    ↓
Q1 (testcontainers) ── 预计 3 天
    ↓
A1 (broker_sim) ────── 预计 4 天
    ↓
A3 (RLS middleware) ─── 预计 1 天
    ↓
A4 (mthub 收敛) ─────── 预计 5 天
    ↓
A2 (StrategyRuntime) ── 预计 5 天（依赖 RS01+RS02 已完成）
```

## 预估

| 梯队 | 项数 | 工期 |
|---|---|---|
| 第一梯队 Q1-Q4 | 4 | ~5 天 |
| 第二梯队 A1-A4 | 4 | ~15 天 |
| **合计** | **8** | **~20 天** |
